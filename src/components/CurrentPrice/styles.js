import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    headerPrice: {
        width: '100%',
        height: 'auto',
        alignItems: 'center',
        marginBottom: 20,
    },
    currentPrice: {
        fontSize: 22,
        color: 'red',
        fontWeight: 'bold',
        marginTop: 10,
        paddingTop: 20,
        marginBottom: 5,
    },
    textPrice: {
        fontSize: 16,
        color: '#fff',
        fontWeight: 'bold',        
    },
});

export default styles;